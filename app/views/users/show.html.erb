<h1><%= @users.full_name %></h1>

<hr>

<h2>Courses</h2>
<ul>
  <% @users.courses.each do |course| %>
  <li><%= link_to course.name, course %></li>
  <% end %>
</ul>

<br>
<br>
<br>

<%= link_to 'Delete User', @user, method: :delete, class: 'ui red button inverted' %>